import numpy as np
import pandas as pd


# class CPT:
#     def __init__(self, states, values):
#         states = list(map(lambda s: frozenset(sorted(s)), states))
#         self.states = sorted(states)
#         self.values = values
#
#     def